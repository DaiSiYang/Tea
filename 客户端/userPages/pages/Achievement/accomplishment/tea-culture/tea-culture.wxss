/* pages/accomplishment/tea-culture/tea-culture.wxss */
.container {
  padding: 30rpx;
  background-color: #f3f7f7;
  min-height: 100vh;
}

.header {
  text-align: center;
  padding: 60rpx 0;
  margin-bottom: 40rpx;
  position: relative;
}

.header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 80rpx;
  height: 3rpx;
  background: linear-gradient(90deg, transparent, #8B4513, transparent);
}

.title {
  font-size: 48rpx;
  font-weight: bold;
  color: #4a2f1d;
  letter-spacing: 6rpx;
  position: relative;
  display: inline-block;
}

.subtitle {
  font-size: 28rpx;
  color: #8B4513;
  margin-top: 16rpx;
  display: block;
  opacity: 0.8;
}

.culture-list {
  padding: 20rpx;
}

.culture-item {
  background: #fff;
  margin-bottom: 40rpx;
  border-radius: 24rpx;
  box-shadow: 0 10rpx 30rpx rgba(139, 69, 19, 0.05);
  overflow: hidden;
  border: 1rpx solid rgba(139, 69, 19, 0.1);
  animation: fadeInUp 0.6s ease forwards;
}

.culture-image {
  width: 100%;
  height: 400rpx;
  display: block;
}

.culture-info {
  padding: 30rpx;
}

.culture-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #4a2f1d;
  margin-bottom: 16rpx;
  display: block;
}

.culture-desc {
  font-size: 28rpx;
  color: #666;
  line-height: 1.6;
  display: block;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.culture-item:nth-child(1) { animation-delay: 0.2s; }
.culture-item:nth-child(2) { animation-delay: 0.3s; }
.culture-item:nth-child(3) { animation-delay: 0.4s; }